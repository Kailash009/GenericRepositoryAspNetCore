@model IEnumerable<GenericRepoNeetu.Models.Teacher>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tAge)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tCity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tAge)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tCity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.tid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.tid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.tid" onclick="return confirm('Are you Sure you want to delete(Y/N)?');">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
